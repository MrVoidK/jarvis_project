# Faz 6.6 — Execution Modes (Scheduled & Continuous) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jarvis'e cron-zamanlı (`scheduled`) ve koşul-izleyen (`continuous`) iki yeni girdi kaynağı ekle; bunlardan gelen MEDIUM+ risk eylemleri otomatik çalıştırmak yerine `tasks` tablosuna "bekleyen onay" olarak kaydet.

**Architecture:** İki yeni üretici modül (`core/scheduler.py`, `core/continuous_runner.py`) kendi daemon thread'lerinde çalışıp `InputHub`'ın ortak kuyruğuna `InputEvent` yazar — mevcut `_handle_turn → guardrail → dispatcher → _run_tool_pipeline` zinciri değişmez. Tek fark: olayın `source` alanı `_run_tool_pipeline`'a taşınır ve orada, onay noktasında, `source ∈ {scheduled, continuous}` + MEDIUM+ ise araç çalıştırılmaz; `core/pending_tasks.py` ile bir `pending` kayıt yazılır. `voice`/`text` davranışı bit-bit korunur (`source` varsayılanı `"voice"`).

**Tech Stack:** Python 3, `croniter` (YENİ — cron ifade parse), `PyYAML` (mevcut), `sqlite3` (stdlib, `core/db.py` üzerinden), `pytest`.

**Spec:** `docs/superpowers/specs/2026-08-28-faz-6.6-execution-modes-design.md`

## Global Constraints

- **Fail-soft (pending_tasks):** `core/memory.py` deseni — her DB istisnası `logger.warning` + güvenli varsayılan (`record_pending → None`, `list_pending → []`). Jarvis'in ana döngüsü asla etkilenmez.
- **Opt-in:** `config/scheduled_tasks.yaml` YOKSA `load_scheduled_config()` `([], [])` döner ve hiçbir yeni thread başlamaz. Dosya VARSA ama bozuk/eksik alanlı/geçersiz cron/bilinmeyen watcher `type` → `ValueError` (boot'ta fail-loud; `core/security_config.py` deseni).
- **Yeni güvenlik yolu yok:** `scheduled`/`continuous` olaylar mevcut guardrail/dispatcher/risk zincirinden aynen geçer (v2 §5).
- **`voice`/`text` regresyon yok:** üç fonksiyona eklenen `source` parametresinin varsayılanı `"voice"`, `event_text` varsayılanı `""`.
- **Scheduled/continuous'tan MEDIUM+ eylem asla otomatik çalışmaz** — kayıt + kullanıcı dönünce karar (Zero-Trust "varsayılan RED"in uzantısı).
- **Daemon thread + `stop_event.wait()`:** Scheduler/Runner uyanık beklemesi `stop_event.wait(poll)` ile kesilebilir — graceful shutdown korunur.
- **Testler hermetik:** gerçek mikrofon/model/ağ yok. Enjeksiyon dikişleri: `Scheduler(now_fn=...)`, `ContinuousRunner(watchers=[...])`, `record_pending(db_path=...)` / `list_pending(db_path=...)`, `load_scheduled_config(path=...)`.
- **Cron yorumu yerel saatte:** Scheduler iç saati naive `datetime.now()` (croniter varsayılanı). `tasks.ts` ise UTC ISO-8601 (pending_tasks'ta).
- Çalıştırma: `python -m pytest tests/ -v` (repo kökünden — bare `pytest` değil).
- Commit mesajı sonu: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Her task sonunda commit **ve** push (kullanıcı tercihi: adım adım, batch değil).

---

## File Structure

| Dosya | Sorumluluk | Task |
|---|---|---|
| `requirements.txt` | `croniter` bağımlılığı | 1 |
| `core/pending_tasks.py` (YENİ) | `tasks` tablosuna fail-soft `record_pending` / `list_pending` | 2 |
| `tests/test_pending_tasks.py` (YENİ) | ^ testleri | 2 |
| `core/input_hub.py` (MODIFY) | `InputSource` += `scheduled`/`continuous`; `submit_event()` public metot | 3 |
| `tests/test_input_hub.py` (MODIFY) | `submit_event` testleri | 3 |
| `core/scheduler.py` (YENİ) | `ScheduledTask`, `WatcherSpec`, `load_scheduled_config`, `Scheduler` | 4 |
| `tests/test_scheduler.py` (YENİ) | ^ testleri | 4 |
| `core/continuous_runner.py` (YENİ) | `ContinuousWatcher` ABC, `FileMtimeWatcher`, `build_watchers`, `ContinuousRunner` | 5 |
| `tests/test_continuous_runner.py` (YENİ) | ^ testleri | 5 |
| `core/app.py` (MODIFY) | `source`/`event_text` parametre zinciri + risk kapısı + `_PENDING_RECORDED_MESSAGES` | 6 |
| `tests/test_app_risk_gate.py` (YENİ) | risk kapısı davranışı | 6 |
| `core/cli_commands.py` (MODIFY) | `/status`'ta bekleyen onaylar | 7 |
| `tests/test_cli_commands.py` (MODIFY) | ^ testi | 7 |
| `core/app.py` (MODIFY, `run_jarvis`) | Scheduler/ContinuousRunner wiring | 8 |
| `config/scheduled_tasks.example.yaml` (YENİ) | şablon (commit'lenir) | 8 |
| `.gitignore` (MODIFY) | `config/scheduled_tasks.yaml` | 8 |
| `docs/mimari-genel-bakis.md` (MODIFY) | §19 thread haritası | 8 |
| `docs/ROADMAP.md` (MODIFY) | §6.6 kutucuklar | 8 |
| `CLAUDE.md` (MODIFY) | §7 durum satırı | 8 |
| `docs/superpowers/specs/2026-08-28-faz-6.6-execution-modes-design.md` (MODIFY) | durum → uygulandı | 8 |

---

## Task 1: `croniter` bağımlılığı

**Files:**
- Modify: `requirements.txt`

**Interfaces:**
- Produces: `croniter` paketi venv'de kurulu; `from croniter import croniter` çalışır.

- [ ] **Step 1: `requirements.txt`'e ekle**

`sentence-transformers==5.1.1` satırının altına, benzer bir yorumla:

```
# Faz 6.6 execution modes: cron ifade parse (scheduler). APScheduler REDDEDILDI
# (asiri agir - kendi thread pool'u/job store'u; bize sadece "kuyruga metin koy"
# lazim). Elde yazilmis cron parser REDDEDILDI (kirilgan).
croniter
```

- [ ] **Step 2: Kur**

Run: `venv/Scripts/pip install croniter`
Expected: başarılı kurulum (croniter + `python-dateutil` bağımlılığı — zaten kurulu olabilir).

- [ ] **Step 3: Import doğrula**

Run: `venv/Scripts/python.exe -c "from croniter import croniter; print(croniter.is_valid('0 8 * * *'))"`
Expected: `True`

- [ ] **Step 4: Mevcut suite hâlâ yeşil**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `189 passed, 1 skipped` (Faz 6.5.1 sonrası taban).

- [ ] **Step 5: Commit + push**

```bash
git add requirements.txt
git commit -m "build(6.6): croniter bagimliligi (execution modes scheduler icin)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 2: `core/pending_tasks.py` — bekleyen onay kaydı

**Files:**
- Create: `src/jarvis/core/pending_tasks.py`
- Test: `tests/test_pending_tasks.py`

**Interfaces:**
- Consumes: `src.jarvis.core.db` — `get_connection(db_path: str | None = None) -> sqlite3.Connection`, `write_lock() -> threading.Lock`. `tasks` tablosu (Faz 6.5.1'de kuruldu): `id, ts, source, text, status, detail_json`; `status` CHECK `('pending','approved','denied','done','error')`.
- Produces:
  - `record_pending(source: str, text: str, detail: dict, db_path: str | None = None) -> Optional[int]` — `status='pending'` satır yazar, `id` döner; her hata → `None` (fail-soft).
  - `list_pending(limit: int = 10, db_path: str | None = None) -> list[dict]` — `status='pending'` satırlar, en yeni önce (`id DESC`), `limit` uygulanır; her hata → `[]`.

- [ ] **Step 1: Failing test — round-trip**

`tests/test_pending_tasks.py`:

```python
"""core/pending_tasks.py - tasks tablosuna fail-soft bekleyen-onay kaydi."""

import pytest

from src.jarvis.core import db as db_module
from src.jarvis.core.pending_tasks import record_pending, list_pending


def test_record_pending_writes_row(tmp_path):
    dbp = str(tmp_path / "t.db")
    task_id = record_pending("scheduled", "gunluk ozet", {"tool": "notes", "risk": "medium"}, db_path=dbp)
    assert isinstance(task_id, int)

    conn = db_module.get_connection(db_path=dbp)
    row = conn.execute("SELECT source, text, status, detail_json FROM tasks WHERE id = ?", (task_id,)).fetchone()
    assert row["source"] == "scheduled"
    assert row["text"] == "gunluk ozet"
    assert row["status"] == "pending"
    assert '"tool": "notes"' in row["detail_json"]


def test_list_pending_newest_first_and_limit(tmp_path):
    dbp = str(tmp_path / "t.db")
    for i in range(4):
        record_pending("continuous", f"olay {i}", {}, db_path=dbp)
    rows = list_pending(limit=2, db_path=dbp)
    assert [r["text"] for r in rows] == ["olay 3", "olay 2"]


def test_list_pending_empty_db(tmp_path):
    assert list_pending(db_path=str(tmp_path / "t.db")) == []


def test_record_pending_failsoft(monkeypatch):
    from src.jarvis.core import pending_tasks

    def boom(*a, **k):
        raise RuntimeError("db down")

    monkeypatch.setattr(pending_tasks.db_module, "get_connection", boom)
    assert pending_tasks.record_pending("scheduled", "x", {}) is None
    assert pending_tasks.list_pending() == []
```

- [ ] **Step 2: Run — verify fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_pending_tasks.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'src.jarvis.core.pending_tasks'`

- [ ] **Step 3: Implement `core/pending_tasks.py`**

```python
"""data/jarvis.db'deki `tasks` tablosuna (Faz 6.5.1) ince erisim - scheduled/
continuous kaynakli MEDIUM+ eylemlerin "bekleyen onay" kaydi.

FAIL-SOFT (core/memory.py deseniyle ayni, BILINCLI): her DB istisnasi ->
logger.warning + guvenli varsayilan (record_pending -> None, list_pending ->
[]). Bir zamanlanmis turun kaydi tutulamazsa Jarvis'in ana dongusu ETKILENMEZ
- kayit, kullaniciya bilgi amacli; guvenlik karari zaten "calistirma"dir.
"""

import json
import logging
from datetime import datetime, timezone
from typing import Optional

from src.jarvis.core import db as db_module

logger = logging.getLogger("jarvis.core.pending_tasks")


def record_pending(
    source: str, text: str, detail: dict, db_path: Optional[str] = None
) -> Optional[int]:
    """`tasks` tablosuna status='pending' bir satir yazar, id doner. Hata -> None."""
    try:
        ts = datetime.now(timezone.utc).isoformat()
        detail_json = json.dumps(dict(detail or {}), ensure_ascii=False)
        conn = db_module.get_connection(db_path=db_path)
        with db_module.write_lock():
            cur = conn.execute(
                "INSERT INTO tasks (ts, source, text, status, detail_json) "
                "VALUES (?, ?, ?, 'pending', ?)",
                (ts, source, text, detail_json),
            )
            conn.commit()
        return int(cur.lastrowid)
    except Exception as exc:  # noqa: BLE001 - fail-soft mutlak (bkz. modul docstring'i)
        logger.warning("record_pending basarisiz (yok sayildi): %s", exc)
        return None


def list_pending(limit: int = 10, db_path: Optional[str] = None) -> list[dict]:
    """status='pending' satirlar, en yeni once. Hata -> []."""
    try:
        conn = db_module.get_connection(db_path=db_path)
        rows = conn.execute(
            "SELECT id, ts, source, text, status, detail_json FROM tasks "
            "WHERE status = 'pending' ORDER BY id DESC LIMIT ?",
            (max(int(limit), 0),),
        ).fetchall()
        return [dict(r) for r in rows]
    except Exception as exc:  # noqa: BLE001 - fail-soft
        logger.warning("list_pending basarisiz (bos liste dondu): %s", exc)
        return []
```

- [ ] **Step 4: Run — verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_pending_tasks.py -q`
Expected: `4 passed`

- [ ] **Step 5: Tam suite**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `193 passed, 1 skipped`

- [ ] **Step 6: Commit + push**

```bash
git add src/jarvis/core/pending_tasks.py tests/test_pending_tasks.py
git commit -m "feat(6.6): core/pending_tasks.py - fail-soft bekleyen-onay kaydi

tasks tablosuna (6.5.1) record_pending/list_pending. Her DB istisnasi
yutulur (memory.py deseni) - zamanlanmis bir turun kaydi tutulamazsa
ana dongu etkilenmez.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 3: `core/input_hub.py` — `InputSource` genişletme + `submit_event`

**Files:**
- Modify: `src/jarvis/core/input_hub.py` — satır 62 (`InputSource`), satır ~162-181 (`submit_external_text`)
- Test: `tests/test_input_hub.py` (mevcut dosyaya ekleme)

**Interfaces:**
- Consumes: mevcut `InputHub` (`_queue: queue.Queue[InputEvent]`, `next_event(poll_interval)`), `InputEvent(source, text)` dataclass.
- Produces:
  - `InputSource = Literal["voice", "text", "scheduled", "continuous"]`
  - `InputHub.submit_event(event: InputEvent) -> None` — herhangi bir üretici thread'inden çağrılabilir; boş/whitespace metinli olay sessizce atılır.
  - `InputHub.submit_external_text(text)` artık `self.submit_event(InputEvent("text", text))` sarmalayıcısı.

- [ ] **Step 1: Failing test — `submit_event`**

`tests/test_input_hub.py` sonuna ekle (mevcut import'lar: `from src.jarvis.core.input_hub import InputEvent, InputHub`, `import threading`):

```python
def test_submit_event_enqueues_any_source():
    hub = InputHub(threading.Event())
    hub.submit_event(InputEvent(source="scheduled", text="gunluk ozet"))
    evt = hub.next_event(poll_interval=0.05)
    assert evt.source == "scheduled" and evt.text == "gunluk ozet"


def test_submit_event_skips_blank():
    hub = InputHub(threading.Event())
    hub.submit_event(InputEvent(source="continuous", text="   "))
    assert hub._queue.empty()


def test_submit_external_text_still_wraps_to_text_source():
    hub = InputHub(threading.Event())
    hub.submit_external_text("merhaba")
    evt = hub.next_event(poll_interval=0.05)
    assert evt.source == "text" and evt.text == "merhaba"
```

- [ ] **Step 2: Run — verify fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_input_hub.py -q -k submit`
Expected: FAIL — `AttributeError: 'InputHub' object has no attribute 'submit_event'` (ilk iki test); üçüncü zaten geçebilir.

- [ ] **Step 3: `InputSource` literal'ini genişlet**

`src/jarvis/core/input_hub.py` satır 62:

```python
InputSource = Literal["voice", "text", "scheduled", "continuous"]
```

- [ ] **Step 4: `submit_event` ekle, `submit_external_text`'i sadeleştir**

Mevcut `submit_external_text` metodunun (satır ~162-181) YERİNE:

```python
    def submit_event(self, event: InputEvent) -> None:
        """Herhangi bir uretici thread'inden (Faz 6.6 scheduler / continuous
        runner, ya da HUD WebSocket thread'i) dogrudan cagrilabilir - `self._queue`
        bir `queue.Queue` (stdlib kendi kilidiyle korur). Bos/whitespace metinli
        olaylar sessizce atilir (gurultu kaynakli tetiklemeler kuyruga girmesin).
        """
        if event.text.strip():
            self._queue.put(event)

    def submit_external_text(self, text: str) -> None:
        """JARVIS HUD (web-ui) WebSocket thread'inden (`core/api.py`) gelen
        yazili bir komutu ana kuyruga ekler - `_text_producer`'in yaptigi AYNI
        sey. Web'den yazilan metin, terminalden yazilanla AYNI
        `InputEvent(source="text", ...)` olarak girer; `run_jarvis()` `/slash`
        komutlarini ve dogal dili aralarinda fark gozetmeden isler, onay bekleme
        sirasinda gelen bir "evet"/"y" de ayni yoldan akar (bkz.
        wait_for_text_answer()).
        """
        self.submit_event(InputEvent(source="text", text=text))
```

- [ ] **Step 5: Run — verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_input_hub.py -q`
Expected: tüm testler PASS (yeni 3 + mevcutlar).

- [ ] **Step 6: Tam suite**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `196 passed, 1 skipped`

- [ ] **Step 7: Commit + push**

```bash
git add src/jarvis/core/input_hub.py tests/test_input_hub.py
git commit -m "feat(6.6): InputSource += scheduled/continuous, InputHub.submit_event()

submit_external_text artik submit_event'in ince sarmalayicisi. Yeni
ureticiler (scheduler, continuous runner) ortak kuyruga bu public metotla
yazar - ayri bir guvenlik yolu yok.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 4: `core/scheduler.py` — cron-zamanlı üretici

**Files:**
- Create: `src/jarvis/core/scheduler.py`
- Test: `tests/test_scheduler.py`

**Interfaces:**
- Consumes: `croniter` (Task 1), `src.jarvis.core.input_hub.InputEvent`, `src.jarvis.core.paths.PROJECT_ROOT`, `yaml`.
- Produces:
  - `ScheduledTask` (frozen dataclass): `name: str`, `cron: str`, `text: str`
  - `WatcherSpec` (frozen dataclass): `name: str`, `type: str`, `params: dict`
  - `load_scheduled_config(path: str = CONFIG_PATH) -> tuple[list[ScheduledTask], list[WatcherSpec]]` — dosya yok → `([], [])`; dosya var + bozuk → `ValueError`.
  - `CONFIG_PATH: str` = `<PROJECT_ROOT>/config/scheduled_tasks.yaml`
  - `Scheduler(hub, stop_event, tasks, poll_interval=30.0, now_fn=<naive local now>)` — `.start()` daemon thread `jarvis-scheduler` açar; `._due(last_check, now) -> list[ScheduledTask]` (test dikişi).

- [ ] **Step 1: Failing test — config yükleme + `_due`**

`tests/test_scheduler.py`:

```python
"""core/scheduler.py - cron-zamanli InputEvent uretici + config yukleme."""

import threading
from datetime import datetime

import pytest

from src.jarvis.core.scheduler import (
    ScheduledTask,
    WatcherSpec,
    Scheduler,
    load_scheduled_config,
)


def _write(p, body: str):
    p.write_text(body, encoding="utf-8")
    return str(p)


def test_load_missing_file_returns_empty(tmp_path):
    assert load_scheduled_config(str(tmp_path / "nope.yaml")) == ([], [])


def test_load_valid_config(tmp_path):
    path = _write(
        tmp_path / "s.yaml",
        "scheduled:\n"
        "  - name: morning\n"
        "    cron: '0 8 * * *'\n"
        "    text: gunluk ozet\n"
        "watchers:\n"
        "  - name: inbox\n"
        "    type: file_mtime\n"
        "    path: jarvis_workspace/inbox.md\n"
        "    text: inbox degisti\n",
    )
    tasks, watchers = load_scheduled_config(path)
    assert tasks == [ScheduledTask("morning", "0 8 * * *", "gunluk ozet")]
    assert watchers == [
        WatcherSpec("inbox", "file_mtime", {"path": "jarvis_workspace/inbox.md", "text": "inbox degisti"})
    ]


def test_load_missing_field_raises(tmp_path):
    path = _write(tmp_path / "s.yaml", "scheduled:\n  - name: x\n    cron: '0 8 * * *'\n")  # text yok
    with pytest.raises(ValueError):
        load_scheduled_config(path)


def test_load_invalid_cron_raises(tmp_path):
    path = _write(tmp_path / "s.yaml", "scheduled:\n  - name: x\n    cron: 'bogus cron'\n    text: y\n")
    with pytest.raises(ValueError):
        load_scheduled_config(path)


def test_load_unknown_watcher_type_raises(tmp_path):
    path = _write(tmp_path / "s.yaml", "watchers:\n  - name: x\n    type: quantum_sensor\n")
    with pytest.raises(ValueError):
        load_scheduled_config(path)


def test_due_detects_cron_hit_once_per_window():
    sched = Scheduler(hub=None, stop_event=threading.Event(),
                      tasks=[ScheduledTask("m", "0 8 * * *", "ozet")])
    before = datetime(2026, 1, 1, 7, 59)
    after = datetime(2026, 1, 1, 8, 1)
    assert [t.name for t in sched._due(before, after)] == ["m"]
    # ayni saat ilerlemezse tetiklenmez
    assert sched._due(after, datetime(2026, 1, 1, 8, 5)) == []
    # iki periyot atlansa bile TEK gorev (birikmez)
    assert [t.name for t in sched._due(datetime(2026, 1, 1, 7, 0), datetime(2026, 1, 1, 10, 0))] == ["m"]


def test_run_thread_honors_stop_event():
    import time

    class _FakeHub:
        def submit_event(self, evt): pass

    stop = threading.Event()
    sched = Scheduler(_FakeHub(), stop, tasks=[], poll_interval=0.01)
    sched.start()
    time.sleep(0.05)
    stop.set()
    sched._thread.join(timeout=1)
    assert not sched._thread.is_alive()


def test_run_submits_event_when_due(monkeypatch):
    events = []

    class _FakeHub:
        def submit_event(self, evt): events.append(evt)

    # now_fn ilk cagrida 07:59, sonraki cagrilarda 08:01 -> ilk poll'da due
    seq = iter([datetime(2026, 1, 1, 7, 59), datetime(2026, 1, 1, 8, 1),
                datetime(2026, 1, 1, 8, 1), datetime(2026, 1, 1, 8, 1)])
    stop = threading.Event()

    def fake_now():
        try:
            return next(seq)
        except StopIteration:
            stop.set()
            return datetime(2026, 1, 1, 8, 1)

    sched = Scheduler(_FakeHub(), stop, tasks=[ScheduledTask("m", "0 8 * * *", "ozet")],
                      poll_interval=0.0, now_fn=fake_now)
    sched.start()
    sched._thread.join(timeout=1)
    assert [e.text for e in events] == ["ozet"]
    assert events[0].source == "scheduled"
```

- [ ] **Step 2: Run — verify fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_scheduler.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'src.jarvis.core.scheduler'`

- [ ] **Step 3: Implement `core/scheduler.py`**

```python
"""Zamanlanmis gorevler - cron ifadesiyle tetiklenen, onceden tanimli komut
metinleri. `config/scheduled_tasks.yaml` (OPT-IN: dosya yoksa ozellik kapali).

Uretici deseni `core/input_hub.py:_mic_producer`/`_text_producer` ile ayni:
kendi daemon thread'inde calisir, `hub.submit_event(InputEvent("scheduled",
text))` ile ortak kuyruga yazar - AYRI bir guvenlik yolu YOK, olay normal
guardrail/dispatcher/risk zincirinden gecer (v2 §5).

APScheduler DEGIL (asiri agir - kendi thread pool'u/job store'u; ihtiyacimiz
"kuyruga metin koy"). Elde yazilmis cron parser DEGIL (kirilgan). `croniter`
(kucuk, saf-Python) ile - import GECIKMELI: croniter kurulu degilse net
`ImportError` yalnizca gercekten cron degerlendirilen yolda cikar.

Ic saat NAIVE `datetime.now()` (yerel saat) - cron ifadeleri yerel saatte
yorumlanir (croniter varsayilani). `tasks.ts` (pending_tasks) ayri: o UTC.
"""

import logging
import threading
from dataclasses import dataclass
from datetime import datetime
from pathlib import Path
from typing import Callable, Optional

import yaml

from src.jarvis.core.input_hub import InputEvent
from src.jarvis.core.paths import PROJECT_ROOT

logger = logging.getLogger("jarvis.core.scheduler")

CONFIG_PATH = str(Path(PROJECT_ROOT) / "config" / "scheduled_tasks.yaml")

_VALID_WATCHER_TYPES = {"file_mtime"}


@dataclass(frozen=True)
class ScheduledTask:
    name: str
    cron: str
    text: str


@dataclass(frozen=True)
class WatcherSpec:
    name: str
    type: str
    params: dict


def _now() -> datetime:
    return datetime.now()  # naive/yerel - bkz. modul docstring'i


def _validate_cron(name: str, expr: str) -> None:
    from croniter import croniter  # gecikmeli import (bkz. modul docstring'i)

    if not croniter.is_valid(expr):
        raise ValueError(f"scheduled task {name!r}: gecersiz cron ifadesi {expr!r}.")


def load_scheduled_config(
    path: str = CONFIG_PATH,
) -> "tuple[list[ScheduledTask], list[WatcherSpec]]":
    """`config/scheduled_tasks.yaml`'i okur.

    Dosya YOKSA -> ([], []) (opt-in ozellik, sessizce kapali). Dosya VARSA ama
    bozuk / eksik alanli / gecersiz cron / bilinmeyen watcher type -> ValueError
    (core/security_config.py deseni: "dosyayi sen olusturdun, dogru olmali").
    """
    p = Path(path)
    if not p.is_file():
        return [], []

    try:
        raw = yaml.safe_load(p.read_text(encoding="utf-8")) or {}
    except yaml.YAMLError as exc:
        raise ValueError(f"scheduled_tasks.yaml parse edilemedi: {exc}") from exc
    if not isinstance(raw, dict):
        raise ValueError(
            "scheduled_tasks.yaml kokte bir YAML sozlugu olmali ('scheduled:' / 'watchers:')."
        )

    tasks: list[ScheduledTask] = []
    for i, item in enumerate(raw.get("scheduled") or []):
        if not isinstance(item, dict) or not all(item.get(k) for k in ("name", "cron", "text")):
            raise ValueError(f"scheduled[{i}]: 'name', 'cron', 'text' alanlari zorunlu.")
        _validate_cron(str(item["name"]), str(item["cron"]))
        tasks.append(ScheduledTask(str(item["name"]), str(item["cron"]), str(item["text"])))

    watchers: list[WatcherSpec] = []
    for i, item in enumerate(raw.get("watchers") or []):
        if not isinstance(item, dict) or not all(item.get(k) for k in ("name", "type")):
            raise ValueError(f"watchers[{i}]: 'name', 'type' alanlari zorunlu.")
        wtype = str(item["type"]).strip().lower()
        if wtype not in _VALID_WATCHER_TYPES:
            raise ValueError(
                f"watchers[{i}] ({item['name']}): bilinmeyen type {wtype!r} "
                f"(desteklenen: {sorted(_VALID_WATCHER_TYPES)})."
            )
        if wtype == "file_mtime" and not all(item.get(k) for k in ("path", "text")):
            raise ValueError(
                f"watchers[{i}] ({item['name']}): file_mtime icin 'path' ve 'text' zorunlu."
            )
        params = {k: v for k, v in item.items() if k not in ("name", "type")}
        watchers.append(WatcherSpec(str(item["name"]), wtype, params))

    return tasks, watchers


class Scheduler:
    """Cron-tetiklemeli `InputEvent` uretici. Tek daemon thread; `stop_event`
    ile kesilebilir uyku (`stop_event.wait`)."""

    def __init__(
        self,
        hub,
        stop_event: threading.Event,
        tasks: "list[ScheduledTask]",
        poll_interval: float = 30.0,
        now_fn: Callable[[], datetime] = _now,
    ) -> None:
        self._hub = hub
        self._stop_event = stop_event
        self._tasks = list(tasks)
        self._poll_interval = poll_interval
        self._now_fn = now_fn
        self._thread = threading.Thread(target=self._run, name="jarvis-scheduler", daemon=True)

    def start(self) -> None:
        self._thread.start()

    def _due(self, last_check: datetime, now: datetime) -> "list[ScheduledTask]":
        """(last_check, now] araligina denk gelen cron'lari olan gorevler.
        Bir due-window'da birden fazla cron hit olsa bile gorev BIR KEZ doner."""
        from croniter import croniter

        out: list[ScheduledTask] = []
        for task in self._tasks:
            try:
                nxt = croniter(task.cron, last_check).get_next(datetime)
            except Exception as exc:  # noqa: BLE001 - bir bozuk ifade digerlerini durdurmasin
                logger.warning("scheduler: '%s' cron degerlendirilemedi: %s", task.name, exc)
                continue
            if last_check < nxt <= now:
                out.append(task)
        return out

    def _run(self) -> None:
        last_check = self._now_fn()
        while not self._stop_event.wait(self._poll_interval):
            now = self._now_fn()
            for task in self._due(last_check, now):
                logger.info("scheduler: '%s' tetiklendi -> kuyruga", task.name)
                self._hub.submit_event(InputEvent(source="scheduled", text=task.text))
            last_check = now
```

- [ ] **Step 4: Run — verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_scheduler.py -q`
Expected: `8 passed`

- [ ] **Step 5: Tam suite**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `204 passed, 1 skipped`

- [ ] **Step 6: Commit + push**

```bash
git add src/jarvis/core/scheduler.py tests/test_scheduler.py
git commit -m "feat(6.6): core/scheduler.py - cron-zamanli InputEvent uretici

croniter tabanli, tek daemon thread (jarvis-scheduler), stop_event ile
kesilebilir. load_scheduled_config: dosya yok -> ([], []); dosya var ama
bozuk -> ValueError (opt-in fail-loud). _due() test dikisi olarak ayri.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 5: `core/continuous_runner.py` — koşul izleyen üretici

**Files:**
- Create: `src/jarvis/core/continuous_runner.py`
- Test: `tests/test_continuous_runner.py`

**Interfaces:**
- Consumes: `src.jarvis.core.input_hub.InputEvent`, `src.jarvis.core.scheduler.WatcherSpec`.
- Produces:
  - `ContinuousWatcher` (ABC): sınıf attr `name: str`; `check(self) -> Optional[str]` (abstractmethod).
  - `FileMtimeWatcher(name: str, path: str, text: str)` — `check()` dosyanın mtime'ı değiştiyse (bir kez) `text`, dosya yok / değişmemiş → `None`.
  - `build_watchers(specs: list[WatcherSpec]) -> list[ContinuousWatcher]`
  - `ContinuousRunner(hub, stop_event, watchers, poll_interval=5.0)` — `.start()` daemon thread `jarvis-continuous`; `._poll_once()` (test dikişi) her watcher'ı `try/except` ile yoklayıp tetiklenende `hub.submit_event(InputEvent("continuous", text))`.

- [ ] **Step 1: Failing test**

`tests/test_continuous_runner.py`:

```python
"""core/continuous_runner.py - kosul izleyen daemon thread + FileMtimeWatcher."""

import os
import threading
import time

from src.jarvis.core.continuous_runner import (
    ContinuousWatcher,
    FileMtimeWatcher,
    ContinuousRunner,
    build_watchers,
)
from src.jarvis.core.scheduler import WatcherSpec


class _FakeHub:
    def __init__(self):
        self.events = []

    def submit_event(self, evt):
        self.events.append(evt)


def test_file_mtime_watcher_no_change_returns_none(tmp_path):
    f = tmp_path / "a.md"
    f.write_text("x", encoding="utf-8")
    w = FileMtimeWatcher("a", str(f), "degisti")
    assert w.check() is None


def test_file_mtime_watcher_fires_once_on_change(tmp_path):
    f = tmp_path / "a.md"
    f.write_text("x", encoding="utf-8")
    w = FileMtimeWatcher("a", str(f), "degisti")
    future = f.stat().st_mtime + 10
    os.utime(f, (future, future))
    assert w.check() == "degisti"
    assert w.check() is None  # ikinci kez tetiklenmez


def test_file_mtime_watcher_missing_file_returns_none(tmp_path):
    w = FileMtimeWatcher("a", str(tmp_path / "nope.md"), "x")
    assert w.check() is None


def test_build_watchers_creates_file_mtime():
    ws = build_watchers([WatcherSpec("inbox", "file_mtime", {"path": "p.md", "text": "t"})])
    assert len(ws) == 1 and isinstance(ws[0], FileMtimeWatcher) and ws[0].name == "inbox"


def test_poll_once_enqueues_on_trigger():
    class Firing(ContinuousWatcher):
        name = "f"

        def check(self):
            return "olay"

    hub = _FakeHub()
    ContinuousRunner(hub, threading.Event(), [Firing()])._poll_once()
    assert len(hub.events) == 1
    assert hub.events[0].source == "continuous" and hub.events[0].text == "olay"


def test_poll_once_survives_watcher_exception():
    class Boom(ContinuousWatcher):
        name = "boom"

        def check(self):
            raise RuntimeError("patladi")

    class Ok(ContinuousWatcher):
        name = "ok"

        def check(self):
            return "tamam"

    hub = _FakeHub()
    ContinuousRunner(hub, threading.Event(), [Boom(), Ok()])._poll_once()  # raise ETMEMELI
    assert [e.text for e in hub.events] == ["tamam"]


def test_runner_thread_honors_stop_event():
    stop = threading.Event()
    r = ContinuousRunner(_FakeHub(), stop, [], poll_interval=0.01)
    r.start()
    time.sleep(0.05)
    stop.set()
    r._thread.join(timeout=1)
    assert not r._thread.is_alive()
```

- [ ] **Step 2: Run — verify fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_continuous_runner.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'src.jarvis.core.continuous_runner'`

- [ ] **Step 3: Implement `core/continuous_runner.py`**

```python
"""Surekli izleme - bir kosulu (bu fazda: bir dosyanin mtime'i) izleyen daemon
thread; kosul tetiklenince `hub.submit_event(InputEvent("continuous", text))`.

`core/scheduler.py` ile ayni uretici deseni; `stop_event` ile kesilebilir uyku.
CERCEVE (ContinuousWatcher ABC) + TEK somut tip (FileMtimeWatcher). IoT/MCP
kaynaklari ileride ayni ABC'yi implemente eder - `_poll_once()` govdesi degismez.
"""

import logging
import threading
from abc import ABC, abstractmethod
from pathlib import Path
from typing import Optional

from src.jarvis.core.input_hub import InputEvent
from src.jarvis.core.scheduler import WatcherSpec

logger = logging.getLogger("jarvis.core.continuous_runner")


class ContinuousWatcher(ABC):
    """Bir kosulu yoklar; tetiklendiyse uretilecek olay metnini, aksi halde
    None dondurur. `check()` HIZLI olmali (poll dongusunde senkron cagrilir)."""

    name: str

    @abstractmethod
    def check(self) -> Optional[str]:
        raise NotImplementedError


class FileMtimeWatcher(ContinuousWatcher):
    """Bir dosyanin son-degistirilme zamani (mtime) degistiginde tetiklenir.
    Ilk `check()`'te tetiklenmez (kurulustaki mtime taban alinir)."""

    def __init__(self, name: str, path: str, text: str) -> None:
        self.name = name
        self._path = Path(path)
        self._text = text
        self._last_mtime = self._current_mtime()

    def _current_mtime(self) -> Optional[float]:
        try:
            return self._path.stat().st_mtime
        except OSError:
            return None

    def check(self) -> Optional[str]:
        mtime = self._current_mtime()
        if mtime is not None and mtime != self._last_mtime:
            self._last_mtime = mtime
            return self._text
        return None


def build_watchers(specs: "list[WatcherSpec]") -> "list[ContinuousWatcher]":
    """WatcherSpec listesini somut watcher'lara cevirir. Bilinmeyen type zaten
    `scheduler.load_scheduled_config`'te elenmis - buraya yalnizca bilinen
    tipler gelir (else dali savunma amacli)."""
    watchers: list[ContinuousWatcher] = []
    for spec in specs:
        if spec.type == "file_mtime":
            watchers.append(FileMtimeWatcher(spec.name, spec.params["path"], spec.params["text"]))
        else:  # normalde ulasilamaz
            logger.warning("build_watchers: bilinmeyen type atlandi: %s", spec.type)
    return watchers


class ContinuousRunner:
    """Watcher'lari periyodik yoklayan tek daemon thread (jarvis-continuous)."""

    def __init__(
        self,
        hub,
        stop_event: threading.Event,
        watchers: "list[ContinuousWatcher]",
        poll_interval: float = 5.0,
    ) -> None:
        self._hub = hub
        self._stop_event = stop_event
        self._watchers = list(watchers)
        self._poll_interval = poll_interval
        self._thread = threading.Thread(target=self._run, name="jarvis-continuous", daemon=True)

    def start(self) -> None:
        self._thread.start()

    def _poll_once(self) -> None:
        for watcher in self._watchers:
            try:
                result = watcher.check()
            except Exception as exc:  # noqa: BLE001 - bir bozuk watcher thread'i oldurmesin
                logger.warning(
                    "continuous: '%s' check() hatasi: %s", getattr(watcher, "name", "?"), exc
                )
                continue
            if result:
                logger.info("continuous: '%s' tetiklendi -> kuyruga", watcher.name)
                self._hub.submit_event(InputEvent(source="continuous", text=result))

    def _run(self) -> None:
        while not self._stop_event.wait(self._poll_interval):
            self._poll_once()
```

- [ ] **Step 4: Run — verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_continuous_runner.py -q`
Expected: `7 passed`

- [ ] **Step 5: Tam suite**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `211 passed, 1 skipped`

- [ ] **Step 6: Commit + push**

```bash
git add src/jarvis/core/continuous_runner.py tests/test_continuous_runner.py
git commit -m "feat(6.6): core/continuous_runner.py - kosul izleyen uretici

ContinuousWatcher ABC + tek somut FileMtimeWatcher + build_watchers +
ContinuousRunner (jarvis-continuous daemon). Bir watcher check() raise
ederse runner hayatta kalir, digerleri islenir. _poll_once() test dikisi.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 6: `core/app.py` — `source` zinciri + runtime risk kapısı

**Files:**
- Modify: `src/jarvis/core/app.py`
  - satır ~40-75: `_PENDING_RECORDED_MESSAGES` mesaj sözlüğü + `record_pending` import
  - `_handle_turn` (satır 458-466 imza; satır ~530 `_execute_tool` çağrısı)
  - `_execute_tool` (satır 115-123 imza; satır 143-145 `_run_tool_pipeline` çağrısı)
  - `_run_tool_pipeline` (satır 193-201 imza; satır ~271-286 onay bloğu)
  - `run_jarvis` döngüsü (satır ~669 `_handle_turn` çağrısı)
- Test: `tests/test_app_risk_gate.py`

**Interfaces:**
- Consumes: `src.jarvis.core.pending_tasks.record_pending` (Task 2). `src.jarvis.core.risk.requires_approval`. Mevcut `_localized(messages: dict[str,str], lang: str) -> str`, `print_system`, `logger`.
- Produces:
  - `_handle_turn(user_text, history, stop_event=None, input_hub=None, pending=None, on_tool_start=None, speaking_event=None, source: str = "voice")` — generator, davranış `source` dışında değişmez.
  - `_execute_tool(tool, intent, stop_event, input_hub=None, pending=None, on_start=None, speaking_event=None, source: str = "voice", event_text: str = "")`
  - `_run_tool_pipeline(tool, intent, stop_event, input_hub=None, pending=None, on_start=None, speaking_event=None, source: str = "voice", event_text: str = "")` — `source ∈ {scheduled, continuous}` + `requires_approval(tool.risk_level)` → `record_pending(...)` + `print_system` + `_localized(_PENDING_RECORDED_MESSAGES, lang)` döner, aracı çalıştırmaz.

- [ ] **Step 1: Failing test**

`tests/test_app_risk_gate.py`:

```python
"""Faz 6.6 runtime risk kapisi - scheduled/continuous + MEDIUM+ -> pending kayit,
arac calismaz. voice/text davranisi degismez."""

import src.jarvis.core.app as app
from src.jarvis.core.dispatcher import Intent
from src.jarvis.core.risk import RiskLevel
from src.jarvis.tools.base import Tool

_HISTORY = [{"role": "system", "content": "x"}]


class _FakeTool(Tool):
    def __init__(self, name: str, risk: RiskLevel):
        self.name = name
        self.description = "fake"
        self.risk_level = risk
        self.executed = False

    def execute(self, params: dict, stop_event=None) -> str:
        self.executed = True
        return "calisti"


def _stub_dispatch(monkeypatch, tool: _FakeTool):
    monkeypatch.setattr(app, "get_tool", lambda name: tool if name == tool.name else None)
    monkeypatch.setattr(
        app._DISPATCHER,
        "classify",
        lambda text: Intent(name=tool.name, confidence=0.9, source="rule", parameters={"lang": "tr"}),
    )


def test_scheduled_medium_tool_records_pending_and_skips_execution(monkeypatch):
    tool = _FakeTool("danger", RiskLevel.MEDIUM)
    _stub_dispatch(monkeypatch, tool)
    recorded = []
    monkeypatch.setattr(
        app, "record_pending",
        lambda source, text, detail: recorded.append((source, text, detail)) or 7,
    )

    out = list(app._handle_turn("zamanlanmis komut", _HISTORY, source="scheduled"))

    assert tool.executed is False
    assert recorded == [("scheduled", "zamanlanmis komut",
                         {"tool": "danger", "risk": "medium", "params": {}})]
    assert out and "onay" in out[0][0].lower()


def test_scheduled_low_tool_runs(monkeypatch):
    tool = _FakeTool("safe", RiskLevel.LOW)
    _stub_dispatch(monkeypatch, tool)
    monkeypatch.setattr(app, "record_pending", lambda *a, **k: 1)

    list(app._handle_turn("zamanlanmis komut", _HISTORY, source="scheduled"))

    assert tool.executed is True


def test_voice_medium_tool_still_uses_approval_path(monkeypatch):
    tool = _FakeTool("danger", RiskLevel.MEDIUM)
    _stub_dispatch(monkeypatch, tool)
    called = {}
    monkeypatch.setattr(app, "_prompt_for_approval",
                        lambda *a, **k: called.setdefault("asked", True) or False)
    monkeypatch.setattr(app, "record_pending",
                        lambda *a, **k: (_ for _ in ()).throw(AssertionError("voice yolu record_pending cagirmamali")))

    list(app._handle_turn("normal komut", _HISTORY))  # source varsayilani "voice"

    assert called.get("asked") is True
    assert tool.executed is False  # _prompt_for_approval False dondu (onay reddedildi)
```

- [ ] **Step 2: Run — verify fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_app_risk_gate.py -q`
Expected: FAIL — `_handle_turn() got an unexpected keyword argument 'source'` (ilk iki test); üçüncü de `record_pending` attr yok / farklı sebeple.

- [ ] **Step 3: `_PENDING_RECORDED_MESSAGES` + import ekle**

`src/jarvis/core/app.py` — `_APPROVAL_DENIED_MESSAGES` sözlüğünün (satır ~61-64) hemen ardına:

```python
_PENDING_RECORDED_MESSAGES = {
    "tr": "Bu istek onay gerektiriyor; siz dönene kadar beklemeye aldım.",
    "en": "This request needs approval; I've queued it until you're back.",
}
```

Import bloğuna (diğer `from src.jarvis.core...` satırlarının yanına):

```python
from src.jarvis.core.pending_tasks import record_pending
```

- [ ] **Step 4: `_run_tool_pipeline` — imza + risk kapısı**

İmzaya iki parametre ekle (satır 193-201):

```python
def _run_tool_pipeline(
    tool: Tool,
    intent,
    stop_event: Optional[threading.Event],
    input_hub: Optional[InputHub] = None,
    pending: Optional[list[InputEvent]] = None,
    on_start: Optional[Callable[[], None]] = None,
    speaking_event: Optional[threading.Event] = None,
    source: str = "voice",
    event_text: str = "",
) -> str:
```

Onay bloğunun (satır ~272 `if requires_approval(tool.risk_level):`) İÇİNİ, `prompt = ...` satırından ÖNCE şu blokla genişlet:

```python
    # (2) Orta ve uzeri risk -> zorunlu insan onayi (bkz. _prompt_for_approval).
    if requires_approval(tool.risk_level):
        # Faz 6.6: scheduled/continuous kaynakli bir olay insan onayi BEKLEYEMEZ
        # (kullanici klavyede degil). MEDIUM+ eylem otomatik calismaz -> `tasks`
        # tablosuna "pending" kaydi girer, kullanici dondugunde /status'ta gorup
        # karar verir (v2 §5.3; Zero-Trust "varsayilan RED"in dogal uzantisi).
        if source in ("scheduled", "continuous"):
            task_id = record_pending(
                source,
                event_text,
                {"tool": tool.name, "risk": tool.risk_level.value, "params": risky_values},
            )
            ref = f"#{task_id}" if task_id is not None else "(kayit edilemedi, bkz. log)"
            logger.info("[%s] MEDIUM+ eylem onaya alindi %s: %s", source, ref, tool.name)
            print_system(
                f"[{source}] istek '{tool.name}' ({tool.risk_level.value}) onay gerektiriyor "
                f"- otomatik calistirilmadi, beklemede {ref}.",
                level="warning",
            )
            return _localized(_PENDING_RECORDED_MESSAGES, lang)

        prompt = f"'{tool.name}' calistirilsin mi? (risk: {tool.risk_level.value})"
        approved = _prompt_for_approval(
            prompt,
            tool.name,
            tool.risk_level.value,
            risky_values,
            lang,
            input_hub=input_hub,
            pending=pending,
            stop_event=stop_event,
            speaking_event=speaking_event,
        )
        if not approved:
            return _localized(_APPROVAL_DENIED_MESSAGES, lang)
```

> Not: `risky_values` ve `lang` bu fonksiyonda zaten tanımlı (satır ~254-259). Yukarıdaki (1) parametre-guardrail bloğu **değişmeden** önce çalışır — bir scheduled görevin parametresi `rm -rf` içeriyorsa yine reddedilir, pending'e dönüşmez.

- [ ] **Step 5: `_execute_tool` — imza + iletim**

İmza (satır 115-123):

```python
def _execute_tool(
    tool: Tool,
    intent,
    stop_event: Optional[threading.Event],
    input_hub: Optional[InputHub] = None,
    pending: Optional[list[InputEvent]] = None,
    on_start: Optional[Callable[[], None]] = None,
    speaking_event: Optional[threading.Event] = None,
    source: str = "voice",
    event_text: str = "",
) -> str:
```

`_run_tool_pipeline` çağrısı (satır 143-145):

```python
        result = _run_tool_pipeline(
            tool, intent, stop_event, input_hub=input_hub, pending=pending,
            on_start=on_start, speaking_event=speaking_event,
            source=source, event_text=event_text,
        )
```

- [ ] **Step 6: `_handle_turn` — imza + iletim**

İmzaya `source: str = "voice"` ekle (satır 458-466, `speaking_event` parametresinden sonra).

`_execute_tool` çağrısını (satır ~530-538) güncelle:

```python
            result = _execute_tool(
                tool,
                intent,
                stop_event,
                input_hub=input_hub,
                pending=pending,
                on_start=on_tool_start,
                speaking_event=speaking_event,
                source=source,
                event_text=user_text,
            )
```

- [ ] **Step 7: `run_jarvis` döngüsü — `source` geç**

Satır ~669 `_handle_turn(event.text, history, ...)` çağrısına ekle:

```python
                for sentence, lang in _handle_turn(
                    event.text,
                    history,
                    stop_event=stop_event,
                    input_hub=hub,
                    pending=pending,
                    on_tool_start=_stop_spinner_once,
                    speaking_event=speaking_event,
                    source=event.source,
                ):
```

- [ ] **Step 8: Run — verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_app_risk_gate.py -q`
Expected: `3 passed`

- [ ] **Step 9: Tam suite (regresyon)**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `214 passed, 1 skipped` — özellikle mevcut dispatcher/guardrail/tool testleri hâlâ yeşil (voice/text yolu değişmedi).

- [ ] **Step 10: Commit + push**

```bash
git add src/jarvis/core/app.py tests/test_app_risk_gate.py
git commit -m "feat(6.6): runtime risk kapisi - scheduled/continuous + MEDIUM+ -> pending

source/event_text parametreleri _handle_turn -> _execute_tool ->
_run_tool_pipeline zincirinde tasindi (hepsi varsayilan voice/''). Onay
noktasinda scheduled/continuous + requires_approval ise arac CALISMAZ;
record_pending ile tasks'a pending kayit + print_system uyarisi. voice/text
yolu bit-bit degismedi (regresyon suite yesil).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 7: `/status` — bekleyen onayları göster

**Files:**
- Modify: `src/jarvis/core/cli_commands.py` — import bloğu + `_cmd_status` (satır ~116-135)
- Test: `tests/test_cli_commands.py` (mevcut dosyaya ekleme)

**Interfaces:**
- Consumes: `src.jarvis.core.pending_tasks.list_pending(limit, db_path)` (Task 2).
- Produces: `_cmd_status` çıktısına "Bekleyen onaylar (N)" bölümü / "Bekleyen onaylar: yok" satırı.

- [ ] **Step 1: Failing test**

`tests/test_cli_commands.py` sonuna ekle:

```python
def test_status_shows_pending_approvals(monkeypatch, capsys):
    import sys
    import types

    # _cmd_status ears/mouth modullerinden get_active_device'i gecikmeli import
    # eder (gercek modelleri yukler) - testte hafif sahte modullerle degistir.
    monkeypatch.setitem(sys.modules, "src.jarvis.ears.listener",
                        types.SimpleNamespace(get_active_device=lambda: "cpu"))
    monkeypatch.setitem(sys.modules, "src.jarvis.mouth.tts",
                        types.SimpleNamespace(get_active_device=lambda: "cpu"))

    from src.jarvis.core import pending_tasks
    monkeypatch.setattr(
        pending_tasks, "list_pending",
        lambda limit=10, db_path=None: [
            {"id": 3, "source": "scheduled", "text": "gunluk ozet", "status": "pending"}
        ],
    )

    from src.jarvis.core.cli_commands import _cmd_status
    _cmd_status([{"role": "system", "content": "x"}])
    out = capsys.readouterr().out
    assert "#3" in out and "scheduled" in out


def test_status_no_pending_line(monkeypatch, capsys):
    import sys
    import types

    monkeypatch.setitem(sys.modules, "src.jarvis.ears.listener",
                        types.SimpleNamespace(get_active_device=lambda: "cpu"))
    monkeypatch.setitem(sys.modules, "src.jarvis.mouth.tts",
                        types.SimpleNamespace(get_active_device=lambda: "cpu"))
    from src.jarvis.core import pending_tasks
    monkeypatch.setattr(pending_tasks, "list_pending", lambda limit=10, db_path=None: [])

    from src.jarvis.core.cli_commands import _cmd_status
    _cmd_status([{"role": "system", "content": "x"}])
    assert "Bekleyen onaylar" in capsys.readouterr().out
```

> Not: `test_cli_commands.py` başka bir testte `src.jarvis.ears.listener`'ı zaten `sys.modules`'a koyuyorsa `monkeypatch.setitem` onu geçici değiştirir ve test sonunda geri yükler — çakışma olmaz.

- [ ] **Step 2: Run — verify fail**

Run: `venv/Scripts/python.exe -m pytest tests/test_cli_commands.py -q -k status`
Expected: FAIL — çıktıda "Bekleyen onaylar" / "#3" yok.

- [ ] **Step 3: Implement**

`src/jarvis/core/cli_commands.py` import bloğuna (satır ~17-27, diğer `from src.jarvis.core...` yanına):

```python
from src.jarvis.core.pending_tasks import list_pending
```

`_cmd_status` içinde, `for _, tool in sorted(tools.items()): lines.append(...)` döngüsünden SONRA, `print_panel(...)` çağrısından ÖNCE:

```python
    pending_rows = list_pending(limit=5)
    if pending_rows:
        lines.append(f"[bold]Bekleyen onaylar ({len(pending_rows)}):[/bold]")
        for row in pending_rows:
            text = row["text"]
            snippet = (text[:60] + "…") if len(text) > 60 else text
            lines.append(f"  - #{row['id']} · {row['source']} · {snippet}")
    else:
        lines.append("[bold]Bekleyen onaylar:[/bold] yok")
```

- [ ] **Step 4: Run — verify pass**

Run: `venv/Scripts/python.exe -m pytest tests/test_cli_commands.py -q`
Expected: tüm testler PASS (yeni 2 + mevcutlar).

- [ ] **Step 5: Tam suite**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `216 passed, 1 skipped`

- [ ] **Step 6: Commit + push**

```bash
git add src/jarvis/core/cli_commands.py tests/test_cli_commands.py
git commit -m "feat(6.6): /status bekleyen onaylari gosterir

_cmd_status'a list_pending(limit=5) - scheduled/continuous kaynakli MEDIUM+
eylemlerin 'pending' kayitlari (#id · source · metin).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Task 8: Wiring + config + docs + manuel doğrulama

**Files:**
- Modify: `src/jarvis/core/app.py` — `run_jarvis` (satır ~614 `api.register_input_hub(hub)` sonrası)
- Create: `config/scheduled_tasks.example.yaml`
- Modify: `.gitignore` (satır ~28 `config/security.yaml` yanına)
- Modify: `docs/mimari-genel-bakis.md` (§19 thread bölümü — satır ~86-97 + ~192-200)
- Modify: `docs/ROADMAP.md` (§6.6 + §6 Faz başlığı)
- Modify: `CLAUDE.md` (satır 86 §7 durum satırı)
- Modify: `docs/superpowers/specs/2026-08-28-faz-6.6-execution-modes-design.md` (durum satırı)

**Interfaces:**
- Consumes: `src.jarvis.core.scheduler.{Scheduler, load_scheduled_config}`, `src.jarvis.core.continuous_runner.{ContinuousRunner, build_watchers}` (Tasks 4-5).
- Produces: `run_jarvis()` boot'ta opt-in Scheduler + ContinuousRunner başlatır.

- [ ] **Step 1: `run_jarvis` wiring**

`src/jarvis/core/app.py` — `api.register_input_hub(hub)` satırından SONRA, `pending: list[InputEvent] = []` satırından ÖNCE:

```python
    # Faz 6.6 Execution Modes - OPT-IN. config/scheduled_tasks.yaml yoksa
    # load_scheduled_config() ([], []) doner ve hicbir yeni thread baslamaz.
    # Dosya VARSA ama bozuksa ValueError firlar -> run_jarvis burada durur
    # (fail-loud, security.yaml deseni). Import gecikmeli: croniter yalnizca
    # gercekten cron degerlendiren yolda gerekir.
    from src.jarvis.core.scheduler import Scheduler, load_scheduled_config
    from src.jarvis.core.continuous_runner import ContinuousRunner, build_watchers

    scheduled_tasks, watcher_specs = load_scheduled_config()
    if scheduled_tasks:
        Scheduler(hub, stop_event, scheduled_tasks).start()
        print_system(f"{len(scheduled_tasks)} zamanlanmış görev yüklendi (scheduler).", level="info")
    if watcher_specs:
        ContinuousRunner(hub, stop_event, build_watchers(watcher_specs)).start()
        print_system(f"{len(watcher_specs)} sürekli izleyici yüklendi (continuous).", level="info")
```

- [ ] **Step 2: `config/scheduled_tasks.example.yaml`**

```yaml
# Faz 6.6 Execution Modes — bu dosyayi `scheduled_tasks.yaml` olarak kopyalayin
# (gercek dosya .gitignore'da; bu sablon commit'lenir). Dosya YOKSA ozellik
# tamamen kapali - hicbir zamanlayici/izleyici thread'i baslamaz.
#
# Uretilen her komut metni, sesli/yazili bir komutla AYNI guardrail + dispatcher
# + risk zincirinden gecer. scheduled/continuous kaynakli bir MEDIUM+ eylem
# OTOMATIK CALISMAZ - "bekleyen onay" olarak kaydedilir (/status'ta gorunur),
# kullanici dondugunde karar verir.
#
# Her iki bolum de opsiyonel. Bozuk/eksik alan/gecersiz cron/bilinmeyen watcher
# type -> Jarvis boot'ta net bir hatayla durur (opt-in dosya, dogru olmali).

scheduled:
  - name: morning_briefing
    cron: "0 8 * * *"          # her gun 08:00 (YEREL saat)
    text: "günlük özetimi hazırla"

watchers:
  - name: inbox_file
    type: file_mtime            # bu fazdaki TEK tip
    path: "jarvis_workspace/inbox.md"
    text: "gelen kutusu dosyası değişti, işle"
```

- [ ] **Step 3: `.gitignore`**

`config/security.yaml` satırının (satır ~28) hemen ardına:

```
# Faz 6.6 execution modes - kisisel zamanlanmis gorevler/izleyiciler; sablon
# icin bkz. config/scheduled_tasks.example.yaml (bu commit'lenir). security.yaml
# gibi OPSIYONEL: yoksa ozellik sessizce kapali.
config/scheduled_tasks.yaml
```

- [ ] **Step 4: `docs/mimari-genel-bakis.md` §19**

Önce oku: `venv/Scripts/python.exe -c "pass"` yerine — `sed -n '80,100p;185,205p' docs/mimari-genel-bakis.md`.

`hub = InputHub(stop_event, speaking_event); hub.start()   # mic thread + text thread` satırının altına (aynı kod-blok stilinde):

```
Scheduler(...).start()          # jarvis-scheduler daemon (opt-in, cron -> InputEvent source="scheduled")
ContinuousRunner(...).start()   # jarvis-continuous daemon (opt-in, kosul izleme -> source="continuous")
```

Ve producer listesine (satır ~192-200, `_mic_producer` / `submit_external_text` maddelerinin yanına) iki madde:

```
- `core/scheduler.py:Scheduler` — `jarvis-scheduler` daemon; cron ifadesi denk
  gelince `hub.submit_event(InputEvent("scheduled", <onceden tanimli metin>))`.
  `config/scheduled_tasks.yaml` yoksa hic baslamaz.
- `core/continuous_runner.py:ContinuousRunner` — `jarvis-continuous` daemon; bir
  kosulu (bu fazda dosya mtime) izler, tetiklenince
  `hub.submit_event(InputEvent("continuous", ...))`.
```

- [ ] **Step 5: `docs/ROADMAP.md` §6.6**

- Başlık: `### 6.6 Execution Modes — Scheduled & Continuous (v2 Faz F) ⬜` → `✅`
- Üç `- [ ] **...` kutusunu `- [x] **...` yap.
- "Risk kısıtı" maddesine ekle (madde içi, sonuna): "**Boot tarafı zaten Faz 6.4'te** (`registry_loader.py`). **Runtime tarafı bu fazda:** `_run_tool_pipeline`'da `source ∈ {scheduled,continuous}` + `requires_approval` → `core/pending_tasks.py:record_pending()` ile `tasks` tablosuna `pending` kayıt, `/status`'ta görünür. `/approve`/`/deny` tüketimi ertelendi."
- §6 Faz başlığı (satır ~842): "6.1-6.5.1 tamam, 6.6+ bekliyor" → "6.1-6.6 tamam, 6.7+ bekliyor".

- [ ] **Step 6: `CLAUDE.md` §7 durum satırı**

Satır 86'daki `(durum: 6.1-6.5.1 tamam [...], 6.6+ bekliyor — ...)` → `(durum: 6.1-6.6 tamam [... + scheduled/continuous execution modes], 6.7+ bekliyor — ...)`. 200 satır sınırına dikkat: yeni içerik eklerken satırı uzatmaktan kaçın, gerekiyorsa mevcut köşeli-parantez içeriğini kısalt.

- [ ] **Step 7: spec durum satırı**

`docs/superpowers/specs/2026-08-28-faz-6.6-execution-modes-design.md` — `**Durum:** onaylandı, uygulanmayı bekliyor` → `**Durum:** uygulandı (2026-08-28)`.

- [ ] **Step 8: Tam suite**

Run: `venv/Scripts/python.exe -m pytest tests/ -q`
Expected: `216 passed, 1 skipped` (Task 8 kod davranışı eklemez; wiring + docs).

- [ ] **Step 9: Manuel uçtan uca doğrulama**

```bash
# 1. Gecici bir izleyici + yakin-gelecekte bir cron ile gercek config yaz
mkdir -p jarvis_workspace
printf 'test\n' > jarvis_workspace/inbox_test.md
# scheduled_tasks.yaml: watchers -> inbox_test.md, scheduled -> cron "su anki dakika+2"
```

`config/scheduled_tasks.yaml` (elle, gerçek): bir `file_mtime` watcher `jarvis_workspace/inbox_test.md` üzerine (`text: "izleyici testi"`) + bir `scheduled` görev cron'u şimdiden ~2 dk sonraya.

```bash
venv/Scripts/python.exe -u main.py
```

Doğrula:
- Boot'ta "1 zamanlanmış görev yüklendi" + "1 sürekli izleyici yüklendi" yazıyor.
- Başka bir terminalde `printf 'degisti\n' >> jarvis_workspace/inbox_test.md` → ~5 sn içinde Jarvis "izleyici testi" turunu işliyor (log/konsol).
- ~2 dk sonra scheduled görev tetikleniyor.
- `/status` yazıp bekleyen onay bölümünü gör (eğer test görevi bir MEDIUM+ araca çözülüyorsa; değilse en az "Bekleyen onaylar: yok" satırı görünür).
- `Ctrl+C` → temiz kapanış (scheduler/continuous thread'leri asılı kalmıyor).

Sonra: `config/scheduled_tasks.yaml` ve `jarvis_workspace/inbox_test.md` sil (veya `.yaml`'ı minimal/boş bırak) — bunlar `.gitignore`'da, commit'e girmez.

- [ ] **Step 10: Commit + push**

```bash
git add src/jarvis/core/app.py config/scheduled_tasks.example.yaml .gitignore \
        docs/mimari-genel-bakis.md docs/ROADMAP.md CLAUDE.md \
        docs/superpowers/specs/2026-08-28-faz-6.6-execution-modes-design.md
git commit -m "feat(6.6): scheduler/continuous wiring + config sablonu + docs

run_jarvis boot'ta opt-in Scheduler + ContinuousRunner baslatir
(scheduled_tasks.yaml yoksa hicbiri). config/scheduled_tasks.example.yaml
sablon; .gitignore += gercek dosya. mimari §19 thread haritasi, ROADMAP
§6.6 (✅), CLAUDE.md §7 guncellendi. Gercek main.py ile dosya-mtime
watcher + cron turu manuel dogrulandi.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
git push
```

---

## Self-Review

**1. Spec coverage:**

| Spec bölümü | Task |
|---|---|
| §2.1 `InputSource` + `submit_event` | Task 3 |
| §2.2 `core/scheduler.py` (`ScheduledTask`, `WatcherSpec`, `load_scheduled_config`, `Scheduler`, yükleme sözleşmesi tablosu) | Task 4 |
| §2.3 `core/continuous_runner.py` (`ContinuousWatcher`, `FileMtimeWatcher`, `build_watchers`, `ContinuousRunner`) | Task 5 |
| §2.4 `core/pending_tasks.py` (`record_pending`, `list_pending`, fail-soft) | Task 2 |
| §2.5 `core/app.py` risk kapısı (`source`/`event_text` zinciri, `_PENDING_RECORDED_MESSAGES`) | Task 6 |
| §2.6 `/status` | Task 7 |
| §2.7 `run_jarvis()` wiring | Task 8 |
| §2.8 docs + `config/*.example.yaml` + `.gitignore` + `croniter` | Task 1 (croniter), Task 8 (kalan) |
| §3 veri akışı | Task 6 + Task 8 (uçtan uca doğrulama) |
| §4 hata yönetimi | Task 2 (fail-soft), Task 4 (fail-loud load), Task 5 (watcher izolasyon), Task 8 (croniter ImportError) |
| §5 test planı | her task'ın kendi test adımları |
| §7 değişmezler | Global Constraints |

Boşluk yok.

**2. Placeholder scan:** Kod adımlarının hepsinde gerçek kod var; "TBD"/"uygun hata yönetimi ekle" yok. Task 8 Step 4/5/6 doküman düzenlemeleri "önce oku, sonra şu formatta ekle" biçiminde somutlaştırıldı (birebir satır kopyalanamaz çünkü hedef dosyanın o bölümü okunmadan bilinmiyor — talimat + format örneği verildi).

**3. Type consistency:**
- `record_pending(source, text, detail, db_path=None) -> Optional[int]` — Task 2'de tanımlı, Task 6'da (fake ile) ve Task 8'de aynı imzayla çağrılıyor. Task 6 gerçek çağrı: `record_pending(source, event_text, {...})` (3 pozisyonel, `db_path` yok) — imzayla uyumlu.
- `list_pending(limit=10, db_path=None) -> list[dict]` — Task 2 tanım, Task 7 `list_pending(limit=5)`, testlerde `list_pending(db_path=...)` — uyumlu.
- `load_scheduled_config(path=CONFIG_PATH) -> tuple[list[ScheduledTask], list[WatcherSpec]]` — Task 4 tanım, Task 8 argümansız çağrı — uyumlu.
- `Scheduler(hub, stop_event, tasks, poll_interval=30.0, now_fn=_now)` / `._due(last_check, now)` / `._thread` — Task 4 tanım ve testleri tutarlı; Task 8 `Scheduler(hub, stop_event, scheduled_tasks).start()` — uyumlu.
- `ContinuousRunner(hub, stop_event, watchers, poll_interval=5.0)` / `._poll_once()` / `._thread` — Task 5 tanım ve testleri; Task 8 `ContinuousRunner(hub, stop_event, build_watchers(watcher_specs)).start()` — uyumlu.
- `build_watchers(specs: list[WatcherSpec]) -> list[ContinuousWatcher]` — Task 5, Task 8 — uyumlu.
- `InputHub.submit_event(event: InputEvent)` — Task 3 tanım; scheduler/runner (Task 4/5) `hub.submit_event(InputEvent(source=..., text=...))` — uyumlu.
- `WatcherSpec(name, type, params)` — Task 4 tanım; Task 5 `spec.type`, `spec.params["path"]`, `spec.params["text"]`, `spec.name` — uyumlu.
- `_handle_turn(..., source="voice")` / `_execute_tool(..., source="voice", event_text="")` / `_run_tool_pipeline(..., source="voice", event_text="")` — Task 6 içinde üçü de aynı isimlerle; `run_jarvis` (Task 6 Step 7) `source=event.source` — uyumlu.

Tutarsızlık yok.
